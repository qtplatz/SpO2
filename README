==============
SpO2 readme
==============

This is the git repository of SpO2.

=====================
Build SpO2 software
=====================

A part of malpix code is depend on QtPlatz project.  QtPlatz build directory
should be found by CMakeLists.txt on root directory of SpO2 source tree.

--------------------
Build on Windows
--------------------

Follow the command sequence below on the VisualStudio command console;

$ mkdir build
$ cmake -G "Visual Studio 12 Win64" ..

Open SpO2.sln by Visual Studio 2013, and build.

